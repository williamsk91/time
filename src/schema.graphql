type Page {
  id: String
  title: String
  path: [String]
  content: String
}

type Query {
  getPage(id: String!): Page
}

type Mutation {
  createPage(path: [String!]!): Page
  saveContent(pageId: String!, content: String!): String
}
